See AGENTS.md for full project context, architecture, and conventions.

## Key Rules

- Always run `make test` after making changes
- When adding or modifying user-facing features (defcustom, commands), update README.md
- When adding a new defcustom, add it to the `.emacs options` section of README.md
- If the feature warrants its own explanation (like Rust dependency searching), add a dedicated subsection under Configuration
